<%= form_for(@purchase) do |f| %>
  
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new purchase..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>